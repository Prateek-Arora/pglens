# PgLens — Project Status & Tracker

> **Living status doc — the external memory that lets any session recover context fast.** Update the *Current focus* and *Phase tracker* after every meaningful change. This is the first file to read at session start.

_Last updated: 2026-09-26_

## Current focus
**Phases 0–2.6 ✅ SHIPPED** — `v0.0.1` (CLI engine), `v0.0.2` (gRPC agent → server → history), `v0.0.3` (Phase-2 hardening, ADR-0035; PR #6 merged, tagged), `v0.0.4` (Phase 2.5 accuracy sprint + the plan-review fixes; merged, tagged), `v0.0.5` (JOB benchmark follow-ups + Phase 2.6 `pglens confirm`; PR #8 merged, tagged). Full detail per step is in the session log below and `docs/phases/phase_{0,1,2}.md`.

**2026-09-24 — independent plan review (ADR-0036, ADR-0037).** Re-verified the whole plan bundle against the shipped code + fresh research. Verdict: **design sound, strong portfolio piece, modest product** — HypoPG-checked index advice is standard in the field (Dexter, PoWA, Supabase `index_advisor`, Postgres MCP Pro, pganalyze), so the positioning is now the *integrated, history-aware, pgss+hypopg-only, private* loop, not "HypoPG validation". Accuracy: *whether* an index helps is trustworthy; *how much* isn't (generic plans overstate skewed wins); selection is per-query with no write cost; the only oracle is self-authored. **Shipped in this pass:** a PG16+ fail-fast gate (pre-16 refused with a clear message); PG17/18 verified with a new CI `compat` job; a **real PG18 bug fixed** (PG18's pg_stat_statements strips a *leading* comment, so PgLens's `/*pglens:introspection*/` marker stopped hiding its own queries → marker now sits after the first keyword via `DataSources.introspection`); README estimate wording + PG16+ requirement; ADR index completed. **Tests: PG16 full build 164/0; PG17 + PG18 integration 62/62 each.** Shipped in `v0.0.4`.

**2026-09-24 — Phase 2.5 (Recommendation Accuracy Sprint) ✅ SHIPPED — `v0.0.4`.** Plan-verified against the real demo DB first (ADR-0038), which **refuted** the planned distribution-weighted estimate (−96.2 % vs the hot-customer −56.7 %) → shipped **value ranges ranked by a worst-case floor**, varying *all* of a query's equality parameters (sampled `pg_stats` values never leave the edge); **HypoPG footprint** + **tuple-level write load** (`table_stats`, V6); **prefix-overlap cross-validation** (`CoverageChecks`) + a per-index **`AdviceService`** read model for Phase 4; `--json` **1.1**. Live demo: `orders(customer_id)` now ranks by −56.7 % (was −98.7 %), and a join index the planner ignores for the hot customer fell from #1 to last. **External benchmark** (`make accuracy`, TPC-H-derived SF 0.1, ADR-0039): capture **16 → 20 of 21** statements after two new capture rewrites; **11 of 14** validated recs cut warm time ≥ 15 % (**79 %** — *superseded 2026-09-25: 6 / 14 under the server's own settings, ADR-0041*); #1 rec est. −73.8 % vs measured −76.0 %; the 3 misses and the buffers→time metric change are published. **Tests: PG16 207/0; PG17 + PG18 integration 71/71.**

**2026-09-25 — second accuracy benchmark, pre-registered (ADR-0040): JOB on the real IMDB snapshot.** `make accuracy-job`. Coverage 99/99; **72 %** of 179 measured recs won (60 % counting 34 recs whose index **can't be built** — B17); **18 % made their query slower** (up to 7.8×; planner misestimates, which HypoPG inherits → B6 matters more); the top of the ranking still finds huge wins (#1: 198 s → 1.0 s). **The value-range floor did not help** by the pre-registered rule (worse error on 6 of 8 recs where it differed). Measured with the server's own settings + `TIMING OFF` (deviations recorded before results; the TPC-H run has the same settings mismatch, disclosed). No engine change. Shipped in `v0.0.5` (PR #8, merge `6fd8fd9`).

**2026-09-25 — acting on JOB (ADR-0041).** Ranking back to the **generic** estimate (the value range is evidence beside it); **B17 build caution** (catalog-only: unbounded key type + TOAST data) → `--json` 1.2, proto field 17, migration **V7**; every report now says **"planner-validated ≠ safe"**; benchmarks measure under the **server's own settings** — which cut the TPC-H precision from 11/14 to **6/14 (43 %)** with 4 recs slower (twice, noise < 3 points). **Phase 2.6 "Confirm on a copy" proposed** (`docs/phases/phase_2_6.md`). Tests: PG16 **220/0**; PG17/18 integration **75/75**. Shipped in `v0.0.5` (PR #8, merge `6fd8fd9`).

**2026-09-25/26 — Phase 2.6 "Confirm on a Copy" approved and built (ADR-0042) → `v0.0.5`.** `pglens confirm --report scan.json --copy <conn> --statements <.sql|PG log>` builds each recommended index on a **user-marked scratch copy** (`pglens.scratch = 'on'` read from `pg_db_role_setting`), times the user's real statements before/after (`READ ONLY` transactions, always rolled back), drops it, and reports faster / no real effect / slower / couldn't be built / not measured — per index **and** per query; confirm JSON 1.0. Step 0 spike first: match by the copy's pg_stat_statements **text** (queryid differs on a copy), replay `$N` via PREPARE/EXECUTE (substitution renumbers constants). Dogfooding found a real bug (the demo's `track = all` records PgLens's own EXPLAIN as a nested row → 0 matches) → top-level rows only, IT now on `track = all`. Demo: PgLens's #1 had **no real effect** (the log queries the hot customer). JOB: 10c reported **slower** (929 ms → 9.2 s) as required; **8/10** index verdicts match the benchmark; the 2 others sit near the threshold and vary between runs (measured, disclosed). Tests: PG16 full build **254/0** (+34); confirm IT on PG17/18 green. Shipped in `v0.0.5` (PR #8, merge `6fd8fd9`).

**2026-09-26 — Phase 3 built + evaluated (ADR-0043); `--plain` defaults to the template (ranking 10–0), `--plain=llm` opt-in.** `docs/phases/phase_3.md` (replaces the Desktop draft). Step 0 spike: 4 small Apache/MIT models via Ollama's OpenAI-compatible API, CPU-only Docker, real TPC-H/JOB findings — **`qwen3.5:4b`** was the only one with no invented number/index/claim (16–34 s per explanation, ~4.9 GiB); granite4:3b invented an index and called a cost estimate an execution-time cut; thinking left on = 211 s and no answer. Plan: runtime-agnostic OpenAI-compatible client in a new `:explain` module (no Spring AI — 2.x needs Boot 4), LLM writes 3 prose fields only, PgLens renders DDL/labels/caveats, unit-aware guard + one retry + template fallback, CLI `--plain` + server cache, rule cards first and pgvector docs retrieval kept only if a pre-registered A/B says so.

**Next action:** Phase 4 (dashboard + REST API): run `docs/workflows/plan-verification.md` on its plan first. **Pre-commitment (flexibility, 2026-09-26):** the dashboard must show a full explanation with **no LLM configured** — compute the template on read (`TemplateExplainer` is pure; `ExplanationInputs` is always on) and use a cached LLM row only when one exists, labeled with its source; every page works with the explain job off. Optional: the user spot-checks 6 of Claude's M3/M4 gradings (`docs/llm-eval.md`). Branch: `phase-3/v0.0.6-rag-llm`.

**Deferred work is tracked in `docs/backlog.md`** (why each item was skipped + contributor tags) — check it before proposing "new" engine features; several are already reasoned through.

## What PgLens is (one line)
OSS, self-hosted Postgres slow-query & index advisor with HypoPG-validated index recs and local-LLM-explained EXPLAIN plans.

## Goals (ranked)
1. A genuinely **usable, self-hostable tool** a team would run (privacy via local LLM + non-hallucinated HypoPG-validated recs).
2. **Target-skill coverage** for the user's job search: RDBMS tuning (deep), gRPC/Protobuf, RAG/pgvector/LLM, Kubernetes, Next.js, Terraform.
3. A clean OSS launch with **real, measured** benchmarks.

## Scope & plan of record
- **Full arc = Phases 0–7**, but **ship after Phase 4** is the plan of record (complete self-hostable web tool proving the four highest-ROI skills). Phases 5–7 = v0.2.
- Calendar: the original **~24–34 weeks** estimate is retired (ADR-0037) — Phases 0–2 took 9 days. Build speed isn't the constraint; **depth of understanding is**, so every phase's DoD includes a teach-back check (explain its key decisions without notes).
- Shippable checkpoints: **Phase 1** `v0.0.1` (CLI) · **Phase 4** `v0.1.0-rc` (web tool — now incl. TLS, dashboard login, registration API) · **Phase 7** `v0.1.0` (public). Tags: Phase 2.5 = `v0.0.4`, Phase 2.6 = `v0.0.5`, Phase 3 = `v0.0.6` (renumbered by ADR-0042; `v0.0.3` was the Phase-2 hardening patch).
- **Supported Postgres:** 16+ (refused below; ADR-0036). CI: PG16 main build + PG17/18 `compat`.

## "Actually usable" acceptance bar (applies to every phase)
1. Read-only & safe by default. 2. One-command self-host (`docker compose up`). 3. No fabricated numbers. 4. Works against a real DB in < 10 min from cold clone. 5. Degrades gracefully (no LLM → still deterministic analysis).

## Phase tracker
| Phase | Title | Status | Ships | Checkpoint |
|---|---|---|---|---|
| −1 | Setup: AI-context system | ✅ done | Docs/memory/token system | — |
| 0 | Foundations & Dev Environment | ✅ done | Repo, Compose, seeded slow demo DB, green CI | `v0.0.0-scaffold` |
| 1 | Core Advisor Engine (CLI) — MVP | ✅ done | `pglens scan`: slow queries + plan + HypoPG-validated rec (full pipeline + R1/R3/R4/R7 + read-only + E2E + CI + ADR-0021 hardening; 82 tests; proven live) — committed `7e22f4e`, tagged | `v0.0.1` ✅ |
| 2 | Collector Agent + Server + History | ✅ done | gRPC agent → server persists snapshots → trends (+ hygiene, BRIN dogfood; v0.0.3 hardening) | `v0.0.2` ✅ · `v0.0.3` ✅ |
| 2.5 | Recommendation Accuracy Sprint | ✅ done | Value-range floors, footprint + write load, overlap cross-validation, TPC-H accuracy benchmark (11/14 pinned → 6/14 under server settings, ADR-0041) | `v0.0.4` ✅ |
| 2.6 | Confirm on a Copy (measured index checks) | ✅ done (ADR-0042) | `pglens confirm`: build recommended indexes on a user-marked scratch copy, run real statements, report measured verdicts | `v0.0.5` ✅ |
| 3 | Plain-Language Explanations (local LLM, grounded + checked) | ✅ shipped `v0.0.6` | Grounded plain-language explanations (local LLM, guardrailed) | `v0.0.6` (was `v0.0.5`; moved by Phase 2.6) |
| 4 | Dashboard (Next.js) | ⬜ | Leaderboard, plan viewer, recs, trends (+opt GraphQL) | `v0.1.0-rc` ← **ship here** |
| 5 | Containerize + Kubernetes + Helm | ⬜ (v0.2) | `helm install pglens` on kind/k3d | — |
| 6 | IaC + Cloud Deploy + Hardening | ⬜ (v0.2) | Terraform → free-tier + Neon; self-observability; opt OIDC | — |
| 7 | Launch & Community | ⬜ (v0.2) | README, benchmarks, demo, `v0.1.0`, launch drafts | `v0.1.0` |

Legend: ⬜ not started · 🟡 in progress · ✅ done · ⏸ paused

## Open questions / to confirm
- ~~Exact Java + Spring Boot versions~~ — **pinned** (Phase 1): Java 21 (Gradle toolchain), Spring Boot 3.5.16, picocli 4.7.7, postgresql 42.7.13, testcontainers 2.0.5 (see `gradle/libs.versions.toml`; ADR-0012).
- ~~Whether to `git init` now~~ — **done** (Phase 0, `main` branch; ADR-0011 context).
- GraphQL read track (Phase 4 optional) — only claim the skill if that track is completed.
- ~~Phase 3: separate `explain-svc` vs an in-server module~~ → an in-JVM `:explain` module (ADR-0043).
- ~~Phase 1: `track=top` vs `track=all` for the ranker~~ — **resolved** (ADR-0014): filter `toplevel = true` + dbid + utility/self, leaving the monitored server's `track` setting untouched.

## Session log (newest first — one line each, keep it short)
- 2026-09-26 — **Secret scanning added.** gitleaks runs as a pre-commit hook (`make hooks`: staged changes, plus a guard that refuses `AGENTS.md`) and as a CI job over the full history (`scripts/secret_scan.sh`, pinned Docker image, no install). The first scan found no leaks in 12 commits or in the 100 pending Phase 3 files, and a manual grep for personal data and paths found none either. The hook was tested: it blocks a fake AWS key and `AGENTS.md`, and allows a clean commit.
- 2026-09-26 — **Phase 3 built + evaluated (ADR-0043).**
  - `:explain` module with pure facts, cards, template and guard, plus the client, endpoint policy and explainer.
  - CLI `--plain` (JSON 1.3).
  - Server: V8 (pgvector HNSW `knowledge_chunks`, `explanations`) and `ExplanationService` plus cache.
  - Compose `llm` profile, `make llm-up` / `llm-eval`, README section, `.claude/rules/explain.md`.
  - Eval on the held-out 12: `qwen3.5:4b` passed the guard first try 12/12, no fallback, M4 12/12, M3 10/12, p50 15.7 s on CPU. Docs-in-prompt scored 2 worse, so docs are links-only. HNSW matched exact 5/5, recall@3 0.60.
  - Dev needed no tuning.
- 2026-09-26 — **Phase 3 approved (ADR-0043); Step 1 done.** 24 eval cases frozen from real demo/TPC-H/JOB reports (`explain/src/test/resources/eval/`), gold key points written before any LLM run, split 12 dev / 12 held-out, decision rules pre-registered in `docs/llm-eval.md`. Refinement: caveats (value range, size, write load, build caution) are PgLens-rendered and kept out of the prompt. Backlog B19–B21.
- 2026-09-26 — **Committed + tagged `v0.0.5`** (PR #8, merge `6fd8fd9`). CI caught fixture files hidden by `.gitignore`'s `*.log` → un-ignored `engine/src/test/resources/confirm/*.log` (tests passed locally only because the files were on disk). Status docs updated on the Phase 3 branch.
- 2026-09-25/26 — **Phase 2.6 built + verified (ADR-0042).** Step 0 spike (PG16/17/18) → text matching + PREPARE/EXECUTE replay + `pg_db_role_setting` marker. Engine: pure `confirm` pkg (`StatementSource`/`SqlSplitter` with real PG16 log fixtures, `QueryText`, `ConfirmPlan`, `Outcome`/`Verdict`, `ConfirmReport`), `db/CopyTarget` (guard) + `db/CopyMeasurer`, facade `CopyConfirmer`; `TargetInfo.port` (JSON 1.2); CLI `confirm` + `ConfirmReportRenderer`. Demo dogfood caught the `track = all` nested-row bug. JOB dogfood: 10c slower ✅, 8/10 verdicts match, rerun quantifies variance. Docs: README "Check the indexes on a copy", architecture, benchmarks, backlog B6 delivered/B7 note, rules, Phase 3 → `v0.0.6`. Shipped in `v0.0.5` (PR #8, merge `6fd8fd9`).
- 2026-09-25 — **ADR-0041 follow-ups.** `RankingScore.drop(generic)` (CLI + server); `BtreeEntryWidth` + validator catalog check → `ValidationResult.buildCaution`, JSON 1.2, proto 17, V7, `IndexAdvice.buildCaution`, CLI ⚠; honesty note; `MEASURE_SETTINGS=server` default; TPC-H re-run ×2 (6/14, 4 slower); JOB per-index view (index-level ρ 0.83) + ADR-0040 top-10 correction; backlog B17 delivered, B18 added; `phase_2_6.md` proposed. PG16 220/0; PG17/18 75/75. Shipped in `v0.0.5` (PR #8, merge `6fd8fd9`).
- 2026-09-25 — **JOB accuracy benchmark (ADR-0040).** Pre-registered in `docs/benchmarks.md`; harness `WORKLOAD=job` (IMDB cached in `build/accuracy-cache/`), `measure.py` gains M1/M2/M5, unbuildable-index handling, one build per distinct index, `MEASURE_SETTINGS=server` + `TIMING OFF`; `--shm-size=1g`. Results: 99/99 coverage, 72 % precision (60 % with unbuildable), 18 % slower, floor not better (6/8 against). Backlog B17 + B6 update. Shipped in `v0.0.5` (PR #8, merge `6fd8fd9`).
- 2026-09-24 — **Phase 2.5 built + verified (ADR-0038, ADR-0039).** Plan-verified on the real demo (weighted-mean idea refuted → value range + floor; privacy: sampled values stay at the edge). Engine: `EqualityParameterLocator`, `ValueSampler` (server-side `quote_literal`; fixed a silent pgjdbc float4→Double failure), `ValueRanges`, `RankingScore`, `IndexFootprint`, `WriteLoad`/`TableActivity`, `CoverageChecks`, `IndexCandidate.parseDdl/covers`, capture rewrites (`extract`→`date_part`, untyped arithmetic). Proto optional fields; agent mappers; server V6 (`recommendations` evidence + `table_stats`), shared score, coverage-check enqueue, `AdviceService`. CLI renders ranges/footprint/write load/coverage; `--json` 1.1. `make accuracy` TPC-H harness: capture 20/21, precision 11/14 by warm time (buffers 2/14 — metric change disclosed). **PG16 207/0; PG17/18 71/71.** Committed on `phase-2.5/v0.0.4-accuracy` → PR → merged, tagged `v0.0.4`. **Next:** Phase 3.
- 2026-09-24 — **Independent plan review + fixes (ADR-0036, ADR-0037).** Reviewed the Desktop plan bundle vs shipped code + web research (competitors, pricing, HypoPG/PG18, Neon provider). Fixed: PG16+ fail-fast gate (`StatsReader.requireSupportedVersion`); monitored image parameterized (`ARG PG_MAJOR`) + ITs take `-PmonitoredImage` + CI `compat` job (PG17/18); **PG18 strips leading comments from pgss text** → `DataSources.introspection` puts the marker after the first keyword (all 7 sites); README "measured cost delta" → estimate wording + PG16+ requirement; ADR index filled (0029–0037); backlog B13–B16; proposed `phase_2_5.md`; Desktop plan bundle corrected (positioning, Neon, tags, Phase-4 security scope, review addendum). **PG16 164/0, PG17 62/62, PG18 62/62.** Shipped with `v0.0.4`. **Next:** user approves Phase 2.5 → plan-verification → build.
- 2026-08-30 — **Phase 2 hardening patch (v0.0.3-in-progress, ADR-0035).** A second independent re-verification (153 tests, re-run fresh; all DoD/charter clean) filtered the residual gaps to *orphans with no downstream owner*. **Fixed:** **G1/F4** — pulled the *reclaim* half of F4 in (revising ADR-0034): `AnalysisRepository.reclaimStuckLeases` in the singleton pass (`LEASED` past `lease-timeout-ms`/5min → `PENDING` +`attempts`; past `max-validation-attempts`/5 → `FAILED` dead-letter), migration **V5** (`attempts` col + `validation_jobs_leased_idx`), an enqueue **poison-cooldown** — so a stuck lease no longer permanently drops a recommendation; **fence-less on purpose** (single-agent-per-db → no concurrent-worker race; the epoch fence stays **B12**). **G6** — `recordResult` ignores a report for a terminal job. **G7** — corrected the "idempotent on `captured_at`" claim (the real guard is the anchor-advance; ADR-0024). **G4** — sharpened "no lost window" to *no lost totals* (missed intervals merge; resolution trade, not data loss). **G5** (literal server-restart IT) considered + **skipped** (dropped-batch IT already proves the intent; OS-assigned port makes it brittle). Server **11 unit / 33 IT** green (+6). ADR-0035 + B12 narrowed + `server.md`/`phase_2.md` updated. **Next:** full `./gradlew build`, then user go-ahead to commit + tag `v0.0.3`.
- 2026-08-30 — **Phase 2 SHIPPED as `v0.0.2` 🎉.** Committed the whole change set (91 files, `a019248`) on branch `phase-2/collector-server-history` → **PR #3** → all CI green (lint + smoke + build across both runs) → merged to `main` (`3898649`) → annotated tag **`v0.0.2`** pushed to `origin`. **Phase 2 DoD fully met.** Next: Phase 3 (RAG + LLM), starting with plan-verification.
- 2026-08-30 — **Phase 2 post-ship audit + hardening (ADR-0034).** An independent end-to-end re-verification (read the real code — delta / ingest / auth / validation / edge-safety / hygiene / queue) confirmed **no charter, safety, or correctness violations**: honesty (NULL-not-0.0, mean=NULL for 0 calls), two-layer read-only, single-thread scheduler over the one monitored conn, HypoPG `finally`-reset (`hypopg()=0`), context-not-wire authz (agent can't inject a DDL), tx-scoped advisory lock — all verified in code. Found + **fixed 3 gaps**: **F1** — re-analysis re-enqueued an already-`DONE` candidate every 30s (unbounded `validation_jobs` + redundant edge HypoPG) → `enqueue` now skips a candidate with a recommendation younger than `revalidate-after-ms` (default 1h) + `pruneTerminalJobs` older than `job-retention-ms` (default 7d), both IT-tested; **F2** — idle queries wrote zero-`calls_delta` rows → `DeltaCalculator` emits none (anchor still advances); **F3** — the "§10 no-lost-window" claim had been conflated with the reset test → added a direct dropped-batch ingest IT. **F4** (a crashed-mid-lease job stuck `LEASED`) **deferred → backlog B12** with a *researched* lease-reclaim + dead-letter design (lease-timeout janitor in the analysis singleton + attempts/dead-letter + fencing token) — a schema+tuning+fencing feature, not an inline patch, and low-harm since validation is idempotent. Server **11 unit / 27 IT** (+1/+3); full `./gradlew build` green. ADR-0034 + backlog B12 recorded; `server.md`/`phase_2.md` updated. No commits yet. **Next: ship `v0.0.2`.**
- 2026-08-29 — **Phase 2 Step 12 (final): CI + docs → `v0.0.2` ready.** CI `lint` job now runs the single-source-of-truth `scripts/lint.sh` (ends local/CI drift): shellcheck +`dogfood_benchmark.sh`, hadolint on all 3 Dockerfiles (agent/server images given numeric **UID 10001** to satisfy DL3066 + a K8s `runAsNonRoot` future), sqlfluff unchanged. `build` job also `docker build`s agent+server images (Dockerfile/COPY guard). **Fixed a latent Step-9 break:** `smoke_test.sh` scoped to the 2 data-plane dbs (`up … monitored-db metadata-db`) so its bare `compose up --build` no longer tries to build the app images with no jars — re-ran smoke green. Wrote self-contained `docs/phases/phase_2.md`; refreshed `docs/architecture.md` (Phase-2 section + topology/RO-role fixes); phases README index updated. **Verified:** full `./gradlew build` = **149 tests (96 unit + 53 IT), 0 fail**; both images build + run non-root (uid 10001); lint + smoke green. ADRs 0023–0033 confirmed recorded. **Phase 2 DoD met except the tag** — `v0.0.2` awaits the user's go-ahead to commit (feature branch → PR). No commits yet. **Next: ship `v0.0.2`, then Phase 3 (RAG + LLM).**
- 2026-08-29 — **Phase 2 Step 11: dogfood benchmark (ADR-0033) → `docs/benchmarks.md` + `V4` BRIN index.** New reproducible harness `scripts/dogfood_benchmark.sh` (`make bench`): throwaway `pgvector:0.8.6-pg16` + real V1–V3, backfill **2.1 M `query_stats` rows** (120q × 26w @ 15-min, 363 MB, `captured_at`-ordered as ingest appends), `EXPLAIN (ANALYZE, BUFFERS)` the verbatim trend SQL before/after each candidate (parallelism+JIT off, median of 5). **Measured:** `windowTotals` top-movers scan rode the V1 PK as a `Nested Loop` of 120 per-query range scans = **~162,961 buffers**/~101 ms; **`BRIN(captured_at)`** → `Bitmap Heap Scan` over ~2,228 contiguous blocks = **~2,238 buffers (~73×)**, 24 kB index; `btree(db_id,captured_at)` matched the read win but 15 MB (~625×) + insert cost. **Decision: `V4__query_stats_trend_brin.sql` = BRIN** (append-only time-ordered column); btree → backlog **B11** (many-tenant only, YAGNI). Honesty: cost=est/exec+buffers=measured; buffers is the cache-independent metric (warm wall-time understates); `newQueries` barely moved (~1.25×, anti-join-bound on the PK — not over-indexed). Fulfilled the V1 dogfood note; updated `TrendRepository`/`server.md` (V1 SQL left immutable). Flyway IT → 4 migrations + BRIN-present check; `:server:build` (24 IT) + shellcheck green. ADR-0033 + B11 recorded. No commits yet. **Next: Step 12 (CI + docs + tag `v0.0.2`).**
- 2026-08-29 — **Phase 2 Step 10: full agent→server→DB integration test.** `AgentToServerFlowIntegrationTest` in `:server` (new test-scoped `project(":agent")` dep — no cycle, :agent never depends on :server) drives the **real** agent runtime (`SampleCollector`, `ValidationRunner`, `IngestClient`/`ValidationClient`) against a booted `PglensServerApplication` + two Testcontainers (monitored image seeded with a 20k-row unindexed `orders`; pgvector metadata). Analysis scheduler parked, `run()` driven directly; agent components constructed + `sample()`/`validatePending()` called directly (deterministic). **4 IT:** (1) two samples → delta persisted, `TrendService.series`+`topMovers` return the query; (2) `pg_stat_statements_reset()` mid-run → post-reset `calls_delta==3` (not negative — §10 self-check); (3) sample→`analysisService.run()` enqueues a btree candidate→`validatePending()` leases+edge-HypoPG+reports→a `PLANNER_VALIDATED` rec persists with `after_cost<before_cost`; (4) wrong token → `IngestException`/`UNAUTHENTICATED`. **Surfaced + fixed a Step-9 latent bug:** disabling the plain `jar` on `:agent` emptied its runtime variant, so `project(":agent")` resolved nothing (compile failed "package com.pglens.agent.* does not exist"); reverted to **keeping the plain jar** — the fixed `app.jar` bootJar name alone makes the image COPY unambiguous (ADR-0032 + Step-9 log corrected). Full `./gradlew build` green (server 10 unit/**23 IT** [+4]; image still builds). No new ADR (exercises ADR-0023/0024/0031). No commits yet. **Next: Step 11 (dogfood benchmark).**
- 2026-08-29 — **Phase 2 Step 9: Docker images + compose for the full loop (ADR-0032).** Hand-written copy-prebuilt Dockerfiles (`deploy/compose/{agent,server}/Dockerfile`): `eclipse-temurin:21-jre-jammy`, non-root, `COPY build/libs/app.jar` (bootJar renamed `app.jar` in both build files → unambiguous, version-proof COPY; plain jar kept enabled). Build context = repo root + `.dockerignore` blocklist. `docker-compose.yml` gains `server` (metadata-db@5432 internal, gRPC :9090, `/dev/tcp` healthcheck, `depends_on` metadata healthy) + `agent` (`pglens_ro`@monitored-db, token, headless, `depends_on` server healthy; 15s sample cadence for demo liveliness). Makefile: `up` builds jars+images+`--wait`; new `register` (inserts `monitored_dbs` row, token hash via pgcrypto `digest(...,'sha256')` = `Tokens.sha256Hex`). **Verified live end-to-end** (`make up`+`register`+`seed`+`warmup`): agent auth's as `pglens_ro`, streams samples; metadata DB got 84 `query_stats` + 45 validation_jobs (30 DONE) + **10 PLANNER_VALIDATED recs w/ real HypoPG estimates** (6442.7→8.6 = 99.9%); clean agent logs, no auth errors; `index_hygiene` correctly empty (demo = guarded PKs only). Gotcha: initdb only runs on a **fresh volume**, so a stale monitored volume lacked `pglens_ro` → `make clean` + up fixes it. Full `./gradlew build` green. ADR-0032 recorded; backlog B10 (registration API) added. No commits yet. **Next: Step 10 (full agent→server→DB IT).**
- 2026-08-29 — **Phase 2 Step 8: DB-level read-only role `pglens_ro` (ADR-0030, closes ADR-0020's deferred item).** New monitored-image initdb `20_pglens_ro.sql`: a `NOSUPERUSER`/no-CREATE*/`NOBYPASSRLS` login role granted only `pg_read_all_stats` + `USAGE`/`SELECT` on `public` (current + future tables) — **no write grant**. hypopg = PUBLIC-executable (session-local), so no grant needed; edge validation runs *as this role* in the read-only tx. CONNECT/USAGE ride PUBLIC defaults → the script names no db (portable across compose `pglens_demo` + Testcontainers). Agent already defaults its URL to `pglens_ro` (`application.yml`). **Defense in depth:** session guard (ADR-0020) *and* role privilege each block writes independently. New `ReadOnlyRoleIntegrationTest` (2 IT vs. the real image: as `pglens_ro` — StatsReader sees the superuser's statement *text* [proves `pg_read_all_stats`], CatalogReader reads, full HypoPG validate → `PLANNER_VALIDATED` + `hypopg()=0`; a plain **unguarded** `pglens_ro` INSERT → "permission denied for table orders"). Harness: `MonitoredDbContainer.initReadOnlyRole` applies the script over JDBC (image doesn't bake initdb). No agent code change (URL was already pointed at the role). sqlfluff clean. **Full `./gradlew build` + all ITs green** (engine 73 unit/29 IT [+2]; other modules unchanged). ADR-0030 recorded. No commits yet. **Next: Step 9 (compose/images).**
- 2026-08-29 — **Phase 2 Step 7: trend queries (ADR-0031).** `TrendRepository` (SQL over `query_stats`) + `TrendService` (window policy + orchestration) answer three questions: **series** — a query's `mean_exec_time`-over-time (`[from,to)`, PK-served); **top movers** — WoW summed `total_exec_time_delta` recent-vs-prior via `FILTER` in one scan, ranked by absolute `deltaMs` (worst regression first); **new-slow** — queries whose *first-ever* row is in the window (`NOT EXISTS` anti-join) past a `HAVING` "slow" floor. **Honesty (charter #1):** derived numbers go through a no-Spring `TrendMath` — `percentChange` is `null` when `prior<=0` (new load has no baseline; a ratio would be a fabricated ∞), `mean` is `null` when `calls=0` (0/0 undefined, never 0). "New" is genuinely-absent-from-history, not merely quiet-last-window, so a returning query is never mislabeled. The cross-query window scans (`windowTotals`/`newQueries`) deliberately hit `captured_at`-range-across-all-queryids — the **Step 11 dogfood target** — left unindexed on purpose (code + `V1__init.sql` note). DTOs: `TrendPoint`/`QueryTrend`/`TopMover`/`NewSlowQuery`. New tests: `TrendMathTest` (3 unit, null boundaries) + `TrendQueryIntegrationTest` (3 IT: regressed>improved ordering + honest null pct; first-in-window slow query w/ real mean, trivial one filtered, established never "new"; series oldest-first). Full REST/GraphQL is Phase 4 — only the service seam the IT drives is built. **Full `./gradlew build` + all ITs green** (server 10 unit/19 IT; other modules unchanged). ADR-0031 recorded. No commits yet. **Next: Step 8 (read-only role, ADR-0030).**
- 2026-08-29 — **Phase 2 Step 6: index hygiene (B5, ADR-0029).** Pure `IndexHygieneAnalyzer` (engine half, no I/O) → `IndexHygieneFinding` of kind `DUPLICATE` (exact twin: same method/columns/predicate), `REDUNDANT` (strict leading prefix of a wider index), `UNUSED` (zero `idx_scan` growth over the window); at most one per index, priority DUPLICATE>REDUNDANT>UNUSED. **Honest "unused":** agent forwards cumulative `idx_scan` in the catalog; `IngestService` appends it to the `index_stats` time-series; `HygieneRepository.scanWindows` deltas first-vs-last per index (≥2 snapshots; a **backwards** delta = a reset = dropped, never "unused"). **One-boolean guard:** `CatalogReader` sets `constraint_backed` from `pg_constraint.conindid` (PK/UNIQUE/EXCLUSION) **or** FK-column coverage (leading-prefix name match) — so `IndexInfo.guarded()` (`unique||primary||constraintBacked`) is the single skip the analyzer honours, and no FK data crosses the wire. `AnalysisService` runs it per db in the singleton pass and **replaces** `index_hygiene` (V3) each time (derived state → a fixed index disappears). Engine `IndexInfo` gained `definition`/`constraintBacked`/`idxScan` (+7-arg compat ctor for detector fixtures). New tests: `IndexHygieneAnalyzerTest` (8 unit incl. guard-never-fires + priority + prefix/method/predicate discrimination), `CatalogReader` IT +2 (PK guarded w/ real definition+idx_scan; an index on FK `orders.customer_id` marked constraint_backed though non-unique), `HygieneFlowIntegrationTest` (3 IT: dup+unused but never a flat-scan PK; re-run clears a resolved dup; a reset ≠ unused), Flyway IT → V3. **Full `./gradlew build` + all ITs green** (engine 73 unit/27 IT, server 7 unit/16 IT, agent 10 unit). ADR-0029 recorded. No commits yet. **Next: Step 7 (trends).**
- 2026-08-29 — **Phase 2 Step 5b: validation round-trip + edge HypoPG + persisted/ranked recs.** Engine seam: `HypoPGValidator.validateDdl(sql, ddl, accessMethod)` → `ValidationResult` (refactor kept the object-path `validate()` behavior; existing HypoPG IT stayed green). Server: `Validation.LeaseValidations` (server-streaming, `UPDATE … FOR UPDATE SKIP LOCKED … RETURNING`) + `ReportValidations` (client-streaming) — auth-gated (an agent leases/reports only its own db); `ValidationRepository.recordResult` upserts `recommendations` (evidence + `estimated_ms_saved` = query total time × relative drop, ADR-0017) and marks the job DONE, atomically. Agent: edge loop `ValidationRunner` (@Scheduled) leases → `validateDdl` next to the DB (hypothetical-only, read-only, reset each candidate) → reports; `ValidationClient` (blocking lease iterator + client-stream report, token on both). **Honesty:** proto `ValidateResult` cost fields made `optional` so NOT_PLANNER_VALIDATED stores NULL, never 0.0 (charter #1). New: `ValidationFlowIntegrationTest` (3 IT over real gRPC+PG: lease→report→persist validated rec w/ score + job DONE; not-validated→NULL costs; unknown-token can't lease), 2 engine `validateDdl` ITs, 2 agent mapper unit tests. **Full `./gradlew build` green across 5 modules.** No new ADR (implements ADR-0023/0017). No commits yet. **Next: Step 6 (hygiene).**
- 2026-08-29 — **Phase 2 Step 5a: scheduled analysis + validation-queue enqueue (ADR-0028).** Server `AnalysisService`: a `@Scheduled` pass, singleton across replicas via a **transaction-scoped advisory lock** (`pg_try_advisory_xact_lock` — pooled-DataSource-correct, unlike session-scoped), reuses the engine **pure half** (`PlanParser`→`AntiPatternDetector`→`IndexCandidateGenerator`) over each db's **persisted catalog** and enqueues candidate DDLs into `validation_jobs`, **idempotently** (partial-unique `ON CONFLICT` arbiter). Catalog now flows agent→server (reversed the Step-4 deferral): agent maps+sends `CatalogSnapshot`; `IngestService` **replaces** the db's `table_catalog`+`index_catalog` each ingest; `CatalogRepository.load` rebuilds the engine `CatalogSnapshot` for detection. **V2 migration** adds `table_catalog` + `index_catalog` detection columns (`columns`/`method`/`predicate`); proto `IndexStat` extended to match. **Queue fills; nothing leases yet (Step 5b).** New: `AnalysisFlowIntegrationTest` (4 IT: catalog round-trip, seq-scan→1 BTREE job enqueued, idempotent re-run, existing-index→no candidate); agent `ProtoMappers` catalog unit test; Flyway IT updated for V2. **Full `./gradlew build` green across 5 modules.** ADR-0028 recorded. No commits yet. **Next: Step 5b.**
- 2026-08-29 — **Phase 2 Step 4: the `:agent` collector + reset-fix re-verification.** First **re-verified the PG16 reset design via research + the real image** (grepped the monitored image: `stats_since` absent from every pgss version file, `default_version=1.10`; PG17 docs confirm a *targeted* `pg_stat_statements_reset(...)` does **not** bump the global `stats_reset` → the per-query regression guard is required, not redundant) and added a **Known-limitation** note to ADR-0024 (the one eviction+recreate-beyond-prior case only PG17 `stats_since` closes). Then built **Step 4**: `StatsReader.globalStatsReset()` (engine; IT-green on real PG16) + the new **`:agent`** Spring Boot module — `@Scheduled` sampler reusing the engine db half, plain-grpc-java `Ingest` client streaming `SampleBatch` with `x-pglens-token` auth, read-only guards re-asserted per cycle, stateless once-per-queryid text+generic-plan registration (marked registered only after a successful send). Pure `ProtoMappers`/`TextHash` unit-tested; agent `bootJar` builds. **Deliberate deviation:** `CatalogSnapshot` → Step 5 (server ignores it until detection reads it). **Full `./gradlew build` green across 5 modules** (agent 7 unit + engine/server ITs incl. new globalStatsReset IT). No new ADR (Step 4 implements ADR-0023/0024/0025/0027); added `.claude/rules/agent.md`. No commits yet. **Next: Step 5.**
- 2026-08-29 — **Phase 2 kickoff: plan-verified + Steps 0–3-core.** Ran plan-verification on the user's Phase 2 plan; **researched & chose the `a-pull` topology** (edge HypoPG via agent-lease; unary + client/server-streaming, no bidi) over live-bidi/replica (ADR-0023), and **corrected the gRPC lib to plain grpc-java** (Spring gRPC's maintained line needs Boot 4.1; ADR-0025, mirrors ADR-0012). **Step 0–1:** `:proto` module + `pglens.proto` contract (Ingest/Validation/Health), codegen green. **Step 2:** `:server` Spring Boot module + Flyway `V1__init.sql` (monitored_dbs, query_texts, query_cumulative, query_stats [dogfood index target], index_catalog/stats, recommendations, validation_jobs + partial queue indexes) — migrations-on-boot proven by a Testcontainers IT (ADR-0026). **Step 3 core:** pure `DeltaCalculator` (server-side ack-anchored deltas + `stats_since` reset/eviction, ADR-0024) with 7 unit tests. **Step 3 done:** plain-grpc-java server lifecycle (`SmartLifecycle`), `Ingest.StreamSnapshot` (client-streaming) → server-side delta persistence, token `AuthInterceptor` (UNAUTHENTICATED on unknown), JDBC repos, Health service — proven by a Testcontainers IT (ingest→delta over real gRPC + real PG, + auth reject). ADR-0023…0027 recorded. **12 server tests green** (7 delta unit + 5 IT); `:server:build` green. **Next: Step 4** (agent — reuse `:engine` db half + gRPC client), then Step 5 (analysis + validation queue), 6 (hygiene), 7 (trends), 8 (RO role), 9 (compose/images), 10 (full IT), 11 (dogfood), 12 (CI/docs/tag). No commits yet.
- 2026-08-27 — **Between-phases hardening + backlog set-up** (reviewed 4 user-raised ideas; ADR-0022). Added a **capture-robustness harness**: a 25-shape "stressor" pack (`engine/src/test/resources/robustness/shape_stressors.sql`) + `PlanCaptureRobustnessTest` that round-trips each through `pg_stat_statements` → capture and asserts a ~0 skip-rate (the ADR-0021 silent-skip class, now regression-guarded) + 7 new `PlanColumnsTest` cases pinning extraction capabilities/boundaries. The harness immediately found one real generic-plan limitation — `= ANY (ARRAY[$1,…])` on a non-text column (untyped `$N`→text ⇒ `integer = ANY(text[])`) — documented as a **surfaced** (not silent) expected-skip → backlog **B9**. Created **`docs/backlog.md`** (deferred work + why + contributor tags): the "get real numbers" track (opt-in ANALYZE on a non-prod target / `auto_explain` / real GIN — B6/B6a/B7), extra rules (BRIN/INCLUDE/expression/partial — B1–B4), LIKE-trigram GIN (B8), type-aware capture (B9). **Pre-committed index-hygiene rec to Phase 2** (B5, owned by Phase 2 — needs the snapshot time-window). Confirmed via HypoPG source that GIN/GiST are fundamentally unsimulatable (btree/brin/hash/bloom only), so the current not-planner-validated labeling is correct. **90 tests green (68 unit + 22 IT)**; full `./gradlew build` green. No commits yet.
- 2026-08-26 — **Phase 1 shipped 🎉.** Committed the full engine/CLI + tests + docs + README to `main` (`7e22f4e`) and tagged **`v0.0.1`** (local; no remote yet). Wrote the README "Try it on your DB in 2 minutes" quickstart (`bootRun` path, since the host JDK is 17 and the toolchain provides 21). Also produced a 3-part teacher walkthrough of Phase 1 (artifacts). 82 tests green. **Phase 1 DoD fully met.** Next: Phase 2 plan-verification.
- 2026-08-26 — **Phase 1 end-to-end re-verification + hardening (ADR-0021).** Unbiased audit vs plan/DoD: build/tests/pure-IO-boundary/safety all green; the 225 IDE "red squiggles" diagnosed as a stale VS Code Gradle sync (Gradle compiles clean), not code. Found the DoD "every demo anti-pattern detected" was **not** truthfully met — **#3** silently skipped: `pg_stat_statements` normalizes `interval '7 days'`→`interval $1`, a **syntax error to EXPLAIN** (drops any temporal-literal query on a real DB). Fixes: `PlanCapturer.normalizeTypedLiterals` (`TYPE $N`→`$N::TYPE`); R1 now detects range/expression predicates (`col >= now() - $1`), still excluding joins; PgLens's own introspection marked `/*pglens:introspection*/` + dropped by hygiene (was polluting its own leaderboard). Cleanup: `DetectionThresholds` (deduped `MIN_TABLE_ROWS`), `StatsReader` error helper, `DataSources` named constants, trimmed verbose javadocs. **82 tests green (61+21)**; proven live (#3 → `btree(created_at)` −50.7% validated + in top recs; 0 self-rows; `hypopg()=0`). No commits yet.
- 2026-08-26 — **Phase 1 step 12 (code-complete).** Enforced **read-only at the DB** on the scan connection (`SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY` in `DataSources.applySessionGuards`; closed a real gap — timeouts-only before); added `PgLensEngine.hypotheticalIndexCount()` safety probe. New **E2E Testcontainers test** `PgLensEngineIntegrationTest` (drives `scan()`/`explain()`; resets pgss for determinism; asserts selective-validated-in-top-list, #11 GIN not-validated-and-excluded, legit full-scan no-rec, `hypopg()=0`, write rejected). CI: new Java `build` job (JDK 21 → build monitored image → `./gradlew build` = Spotless + unit + integration). Phase docs: `phase_1.md`, `.claude/rules/engine.md`, architecture "implemented — Phase 1". ADR-0020. **74 tests green (54 unit + 20 IT)**; `./gradlew build` green. Only `v0.0.1` tag remains. No commits yet.
- 2026-08-26 — **Phase 1 step 11.** R7 `GinCandidateRule` (pure): flags `Seq Scan` filters using GIN-servable containment/existence operators (jsonb `@>`, key-exists `?`/`?|`/`?&`, jsonpath/full-text `@?`/`@@`) on an unindexed column → `IndexCandidateGenerator` emits a **GIN** candidate (separate AM, never merged into a btree composite) → `HypoPGValidator` returns `NOT_PLANNER_VALIDATED` with no fabricated delta. New `PlanColumns.jsonbPredicates` (longest-op-first regex). Demo **#11** GIN fixture added (`events.payload @> '{"ua":"agent-7"}'`); its test fixture is the real captured generic plan. LIKE-trigram GIN deferred (needs pg_trgm; no fixture). ADR-0019. 68 tests green (54 unit + 14 IT); proven live (#11 `[not planner-validated]`, excluded from validated top-recs; `hypopg()=0`, no real indexes). No commits yet.
- 2026-08-25 — **Phase 1 step 10.** Wired the full pipeline into `PgLensEngine.scan()` + `explain(queryid)`; new report models (`ScanReport`/`QueryReport`/`PlanSummary`/`TargetInfo`); `ScanReportRenderer` (human + `--json` v1.0 = model records serialized NON_NULL, ADR-0018); `StatsReader.findByQueryId`. Deleted dead `StatementStatRenderer`. Fixed R4 evidence `.formatted()` precedence bug (+regression test). 62 tests green (48 unit + 14 IT); proven live on demo DB (real deltas, honest suppression, valid JSON, exit codes, hypopg()=0, no real indexes). No commits yet.
- 2026-08-25 — **Phase 1 step 9.** Pure `Recommender` (package `rank`): cross-query ranking score = real pgss total time × validated relative drop (weights a hot-query modest win over a cold-query big win); near-duplicate dedupe by btree-prefix coverage — covered index flagged `subsumedBy`, never dropped, benefits never summed (no fabricated aggregate). New `RankedRecommendation` model. 6 new pure tests; 59 total green (45 unit + 14 IT). ADR-0017. No commits yet.
- 2026-08-25 — **Phase 1 steps 1–8.** Gradle `:engine`/`:cli` (JDK-21 toolchain, Spotless); `StatsReader` (hygiene filter), `PlanCapturer` (GENERIC_PLAN; found+fixed pgjdbc `preferQueryMode=simple`), `PlanParser`/`PlanNode`, `CatalogReader`/`CatalogSnapshot`, pure `AntiPatternDetector` (R1/R3/R4), `IndexCandidateGenerator` (composite = R1+R4), `HypoPGValidator` (used + ≥15% gate; GIN/no-hypopg graceful; no leftover indexes). 53 tests green; validator verified on seeded+ANALYZEd DB. ADR-0012…0016. No commits yet.
- 2026-08-24 — **Phase 0 shipped.** Compose (monitored + metadata) w/ hypopg (PGDG) + pgvector pinned; reproducible skewed seed (~1M rows); slow-query oracle validated with real HypoPG deltas; lint + compose-smoke CI; Apache-2.0; git init on `main`; smoke 8/8. ADR-0009/0010/0011. Tag `v0.0.0-scaffold`.
- 2026-08-22 — Set up AI-context system: `CLAUDE.md`, `docs/` (project, architecture, decisions, phases, workflows, glossary), `.claude/rules/`, memory index. Researched 2026 best practices first.
