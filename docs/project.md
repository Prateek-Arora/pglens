# PgLens — Project Status & Tracker

> **Living status doc — the external memory that lets any session recover context fast.** Update the *Current focus* and *Phase tracker* after every meaningful change. This is the first file to read at session start.

_Last updated: 2026-08-27_

## Current focus
**Phase 1 (Core Advisor Engine CLI): ✅ SHIPPED — committed to `main` (`7e22f4e`) and tagged `v0.0.1`.** Next up: Phase 2 (start with plan-verification on the user's Phase 2 plan). Plan verified (twice) and approved → `.claude/plans/prancy-spinning-panda.md`; decisions recorded as ADR-0012…0021. Two-module Gradle build (`:engine` pure/IO split + `:cli` Spring Boot) on a toolchain-provisioned JDK 21. **Steps 1–12 done + verified:** the full pipeline runs in `PgLensEngine.scan()` — `StatsReader` (rank + hygiene) → `PlanCapturer` (GENERIC_PLAN) → `PlanParser` → `AntiPatternDetector` (**R1**/**R3**/**R4**/**R7**) → `IndexCandidateGenerator` (composite = R1+R4 merge; AM-tagged; **R7 → GIN**) → **`HypoPGValidator`** (used + ≥15% gate, else suppress; graceful GIN/no-hypopg; zero indexes left behind) → **`Recommender`** (real pgss total time × validated relative drop; btree-prefix dedupe). Human report + **`--json` v1.0 contract** (the model records themselves, NON_NULL — can't drift) + **`explain <conn> <queryid>`**. Step 12 adds **DB-enforced read-only** (`SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY` on the one scan connection — writes are *rejected*, closing a real gap; ADR-0020), a full-engine **E2E Testcontainers test** (`scan()`/`explain()`: selective validated + in top list, #11 GIN not-validated + excluded, `GROUP BY status` no validated rec, `hypopg()=0`, read-only rejects writes), the **CI Java `build` job** (JDK 21 → build monitored image → Spotless + unit + integration), and the phase docs (`phase_1.md`, `.claude/rules/engine.md`, architecture). An unbiased end-to-end re-verification (2026-08-26) then hardened it (ADR-0021): fixed a **silent-skip bug** (typed literals like `interval '7 days'` → `interval $1` were a syntax error to EXPLAIN, dropping #3 and any real DB's temporal-literal queries), taught **R1 to detect range/expression predicates**, and stopped PgLens **ranking its own introspection queries** — plus constants/DRY/javadoc cleanup. **82 tests green (61 unit + 21 Testcontainers)**; full `./gradlew build` green. **Proven live** on the demo DB (real deltas incl. #3 now `btree(created_at)` −50.7% validated, honest suppression, #11 GIN labeled, 0 self-pollution rows, exit codes, `hypopg()=0`, no real indexes). Shipped as **`v0.0.1`** (commit `7e22f4e` on `main`, annotated tag pushed to GitHub `origin`) with a README "Try it on your DB in 2 minutes" quickstart.

**Next action:** Phase 2 (collector agent + gRPC + server + history) — begin by running `docs/workflows/plan-verification.md` on the user's Phase 2 plan before any code. (Remote is `origin` = github.com/Prateek-Arora/pglens; `main` + `v0.0.1` are pushed. Work via feature branch → PR.)

**Phase 2 pre-commitments (fold into the plan during plan-verification):**
- **Index hygiene rec — unused + duplicate/redundant indexes** (`pg_stat_user_indexes.idx_scan`, `pg_index` overlap). A catalog-driven, read-only, zero-fabrication advice *class* distinct from the query rules — and it belongs in Phase 2 specifically because "unused" is only honest over a **time window** (idx_scan delta across the snapshots Phase 2 persists), not a single scan. Highest-ROI, most-demoable engine addition (PgHero/`pg-index-health` territory). Tracked as `docs/backlog.md` B5, but **owned by Phase 2** — add it as a first-class deliverable when the plan lands.

**Deferred work is tracked in `docs/backlog.md`** (why each item was skipped + contributor tags) — check it before proposing "new" engine features; several are already reasoned through.

## What PgLens is (one line)
OSS, self-hosted Postgres slow-query & index advisor with HypoPG-validated index recs and local-LLM-explained EXPLAIN plans.

## Goals (ranked)
1. A genuinely **usable, self-hostable tool** a team would run (privacy via local LLM + non-hallucinated HypoPG-validated recs).
2. **Target-skill coverage** for the user's job search: RDBMS tuning (deep), gRPC/Protobuf, RAG/pgvector/LLM, Kubernetes, Next.js, Terraform.
3. A clean OSS launch with **real, measured** benchmarks.

## Scope & plan of record
- **Full arc = Phases 0–7**, but **ship after Phase 4** is the plan of record (complete self-hostable web tool proving the four highest-ROI skills). Phases 5–7 = v0.2.
- Calendar estimate: **~24–34 weeks at ~10 hrs/week** (re-baselined; per-phase build estimates are ~1.5–2× optimistic since gRPC/RAG/K8s/Terraform start from zero).
- Shippable checkpoints: **Phase 1** `v0.0.1` (CLI) · **Phase 4** `v0.1.0-rc` (web tool) · **Phase 7** `v0.1.0` (public).

## "Actually usable" acceptance bar (applies to every phase)
1. Read-only & safe by default. 2. One-command self-host (`docker compose up`). 3. No fabricated numbers. 4. Works against a real DB in < 10 min from cold clone. 5. Degrades gracefully (no LLM → still deterministic analysis).

## Phase tracker
| Phase | Title | Status | Ships | Checkpoint |
|---|---|---|---|---|
| −1 | Setup: AI-context system | ✅ done | Docs/memory/token system | — |
| 0 | Foundations & Dev Environment | ✅ done | Repo, Compose, seeded slow demo DB, green CI | `v0.0.0-scaffold` |
| 1 | Core Advisor Engine (CLI) — MVP | ✅ done | `pglens scan`: slow queries + plan + HypoPG-validated rec (full pipeline + R1/R3/R4/R7 + read-only + E2E + CI + ADR-0021 hardening; 82 tests; proven live) — committed `7e22f4e`, tagged | `v0.0.1` ✅ |
| 2 | Collector Agent + Server + History | ⬜ | gRPC agent → server persists snapshots → trends | — |
| 3 | RAG + LLM Explanation Layer | ⬜ | Grounded plain-language explanations (local LLM, guardrailed) | — |
| 4 | Dashboard (Next.js) | ⬜ | Leaderboard, plan viewer, recs, trends (+opt GraphQL) | `v0.1.0-rc` ← **ship here** |
| 5 | Containerize + Kubernetes + Helm | ⬜ (v0.2) | `helm install pglens` on kind/k3d | — |
| 6 | IaC + Cloud Deploy + Hardening | ⬜ (v0.2) | Terraform → free-tier + Neon; self-observability; opt OIDC | — |
| 7 | Launch & Community | ⬜ (v0.2) | README, benchmarks, demo, `v0.1.0`, launch drafts | `v0.1.0` |

Legend: ⬜ not started · 🟡 in progress · ✅ done · ⏸ paused

## Open questions / to confirm
- ~~Exact Java + Spring Boot versions~~ — **pinned** (Phase 1): Java 21 (Gradle toolchain), Spring Boot 3.5.16, picocli 4.7.7, postgresql 42.7.13, testcontainers 2.0.5 (see `gradle/libs.versions.toml`; ADR-0012).
- ~~Whether to `git init` now~~ — **done** (Phase 0, `main` branch; ADR-0011 context).
- GraphQL read track (Phase 4 optional) — only claim the skill if that track is completed.
- ~~Phase 1: `track=top` vs `track=all` for the ranker~~ — **resolved** (ADR-0014): filter `toplevel = true` + dbid + utility/self, leaving the monitored server's `track` setting untouched.

## Session log (newest first — one line each, keep it short)
- 2026-08-27 — **Between-phases hardening + backlog set-up** (reviewed 4 user-raised ideas; ADR-0022). Added a **capture-robustness harness**: a 25-shape "stressor" pack (`engine/src/test/resources/robustness/shape_stressors.sql`) + `PlanCaptureRobustnessTest` that round-trips each through `pg_stat_statements` → capture and asserts a ~0 skip-rate (the ADR-0021 silent-skip class, now regression-guarded) + 7 new `PlanColumnsTest` cases pinning extraction capabilities/boundaries. The harness immediately found one real generic-plan limitation — `= ANY (ARRAY[$1,…])` on a non-text column (untyped `$N`→text ⇒ `integer = ANY(text[])`) — documented as a **surfaced** (not silent) expected-skip → backlog **B9**. Created **`docs/backlog.md`** (deferred work + why + contributor tags): the "get real numbers" track (opt-in ANALYZE on a non-prod target / `auto_explain` / real GIN — B6/B6a/B7), extra rules (BRIN/INCLUDE/expression/partial — B1–B4), LIKE-trigram GIN (B8), type-aware capture (B9). **Pre-committed index-hygiene rec to Phase 2** (B5, owned by Phase 2 — needs the snapshot time-window). Confirmed via HypoPG source that GIN/GiST are fundamentally unsimulatable (btree/brin/hash/bloom only), so the current not-planner-validated labeling is correct. **90 tests green (68 unit + 22 IT)**; full `./gradlew build` green. No commits yet.
- 2026-08-26 — **Phase 1 shipped 🎉.** Committed the full engine/CLI + tests + docs + README to `main` (`7e22f4e`) and tagged **`v0.0.1`** (local; no remote yet). Wrote the README "Try it on your DB in 2 minutes" quickstart (`bootRun` path, since the host JDK is 17 and the toolchain provides 21). Also produced a 3-part teacher walkthrough of Phase 1 (artifacts). 82 tests green. **Phase 1 DoD fully met.** Next: Phase 2 plan-verification.
- 2026-08-26 — **Phase 1 end-to-end re-verification + hardening (ADR-0021).** Unbiased audit vs plan/DoD: build/tests/pure-IO-boundary/safety all green; the 225 IDE "red squiggles" diagnosed as a stale VS Code Gradle sync (Gradle compiles clean), not code. Found the DoD "every demo anti-pattern detected" was **not** truthfully met — **#3** silently skipped: `pg_stat_statements` normalizes `interval '7 days'`→`interval $1`, a **syntax error to EXPLAIN** (drops any temporal-literal query on a real DB). Fixes: `PlanCapturer.normalizeTypedLiterals` (`TYPE $N`→`$N::TYPE`); R1 now detects range/expression predicates (`col >= now() - $1`), still excluding joins; PgLens's own introspection marked `/*pglens:introspection*/` + dropped by hygiene (was polluting its own leaderboard). Cleanup: `DetectionThresholds` (deduped `MIN_TABLE_ROWS`), `StatsReader` error helper, `DataSources` named constants, trimmed verbose javadocs. **82 tests green (61+21)**; proven live (#3 → `btree(created_at)` −50.7% validated + in top recs; 0 self-rows; `hypopg()=0`). No commits yet.
- 2026-08-26 — **Phase 1 step 12 (code-complete).** Enforced **read-only at the DB** on the scan connection (`SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY` in `DataSources.applySessionGuards`; closed a real gap — timeouts-only before); added `PgLensEngine.hypotheticalIndexCount()` safety probe. New **E2E Testcontainers test** `PgLensEngineIntegrationTest` (drives `scan()`/`explain()`; resets pgss for determinism; asserts selective-validated-in-top-list, #11 GIN not-validated-and-excluded, legit full-scan no-rec, `hypopg()=0`, write rejected). CI: new Java `build` job (JDK 21 → build monitored image → `./gradlew build` = Spotless + unit + integration). Phase docs: `phase_1.md`, `.claude/rules/engine.md`, architecture "implemented — Phase 1". ADR-0020. **74 tests green (54 unit + 20 IT)**; `./gradlew build` green. Only `v0.0.1` tag remains. No commits yet.
- 2026-08-26 — **Phase 1 step 11.** R7 `GinCandidateRule` (pure): flags `Seq Scan` filters using GIN-servable containment/existence operators (jsonb `@>`, key-exists `?`/`?|`/`?&`, jsonpath/full-text `@?`/`@@`) on an unindexed column → `IndexCandidateGenerator` emits a **GIN** candidate (separate AM, never merged into a btree composite) → `HypoPGValidator` returns `NOT_PLANNER_VALIDATED` with no fabricated delta. New `PlanColumns.jsonbPredicates` (longest-op-first regex). Demo **#11** GIN fixture added (`events.payload @> '{"ua":"agent-7"}'`); its test fixture is the real captured generic plan. LIKE-trigram GIN deferred (needs pg_trgm; no fixture). ADR-0019. 68 tests green (54 unit + 14 IT); proven live (#11 `[not planner-validated]`, excluded from validated top-recs; `hypopg()=0`, no real indexes). No commits yet.
- 2026-08-25 — **Phase 1 step 10.** Wired the full pipeline into `PgLensEngine.scan()` + `explain(queryid)`; new report models (`ScanReport`/`QueryReport`/`PlanSummary`/`TargetInfo`); `ScanReportRenderer` (human + `--json` v1.0 = model records serialized NON_NULL, ADR-0018); `StatsReader.findByQueryId`. Deleted dead `StatementStatRenderer`. Fixed R4 evidence `.formatted()` precedence bug (+regression test). 62 tests green (48 unit + 14 IT); proven live on demo DB (real deltas, honest suppression, valid JSON, exit codes, hypopg()=0, no real indexes). No commits yet.
- 2026-08-25 — **Phase 1 step 9.** Pure `Recommender` (package `rank`): cross-query ranking score = real pgss total time × validated relative drop (weights a hot-query modest win over a cold-query big win); near-duplicate dedupe by btree-prefix coverage — covered index flagged `subsumedBy`, never dropped, benefits never summed (no fabricated aggregate). New `RankedRecommendation` model. 6 new pure tests; 59 total green (45 unit + 14 IT). ADR-0017. No commits yet.
- 2026-08-25 — **Phase 1 steps 1–8.** Gradle `:engine`/`:cli` (JDK-21 toolchain, Spotless); `StatsReader` (hygiene filter), `PlanCapturer` (GENERIC_PLAN; found+fixed pgjdbc `preferQueryMode=simple`), `PlanParser`/`PlanNode`, `CatalogReader`/`CatalogSnapshot`, pure `AntiPatternDetector` (R1/R3/R4), `IndexCandidateGenerator` (composite = R1+R4), `HypoPGValidator` (used + ≥15% gate; GIN/no-hypopg graceful; no leftover indexes). 53 tests green; validator verified on seeded+ANALYZEd DB. ADR-0012…0016. No commits yet.
- 2026-08-24 — **Phase 0 shipped.** Compose (monitored + metadata) w/ hypopg (PGDG) + pgvector pinned; reproducible skewed seed (~1M rows); slow-query oracle validated with real HypoPG deltas; lint + compose-smoke CI; Apache-2.0; git init on `main`; smoke 8/8. ADR-0009/0010/0011. Tag `v0.0.0-scaffold`.
- 2026-08-22 — Set up AI-context system: `CLAUDE.md`, `docs/` (project, architecture, decisions, phases, workflows, glossary), `.claude/rules/`, memory index. Researched 2026 best practices first.
