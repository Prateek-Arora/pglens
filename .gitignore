# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
*.iml

# --- Java / Gradle / Maven (added in Phase 0) ---
target/
build/
.gradle/
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar

# --- Node / Next.js (dashboard, Phase 4) ---
node_modules/
.next/
out/
dist/
*.log
# ...except the real PostgreSQL log fixtures the confirm parser is tested on (Phase 2.6)
!engine/src/test/resources/confirm/*.log

# --- Env / secrets ---
.env
.env.*
!.env.example
*.pem
*.key

# --- Local infra / data ---
pgdata/
*.local

# --- Claude Code (keep shared config, ignore local overrides) ---
.claude/settings.local.json
