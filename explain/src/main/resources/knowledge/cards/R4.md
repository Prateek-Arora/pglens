Sort for a LIMIT (R4)
The query sorts many rows (ORDER BY) only to return the first few (LIMIT). Postgres must read and sort
all candidate rows before it can return any of them. A B-tree index in the ORDER BY order lets Postgres
read rows already sorted and stop as soon as it has enough.
Docs: https://www.postgresql.org/docs/16/indexes-ordering.html
