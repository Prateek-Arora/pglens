Unindexed join key (R3)
A join matches rows of two tables on a column. When the column on one side has no index, Postgres must
scan that whole table to find the matching rows (with a hash join or a scan it repeats). An index on
the join column lets Postgres look up only the rows that match each key from the other side.
Docs: https://www.postgresql.org/docs/16/planner-optimizer.html
Docs: https://www.postgresql.org/docs/16/indexes-intro.html
